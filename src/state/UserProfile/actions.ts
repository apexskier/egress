import { createAction } from "redux-actions";

export const SetName = createAction("SET_NAME");
export const SetEmailAddress = createAction("SET_EMAIL_ADDRESS");
export const SetPhone = createAction("SET_PHONE");
